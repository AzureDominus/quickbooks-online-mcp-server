name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        # Node 22.6.0+ required for --experimental-strip-types
        # Node 23.6.0+ has it enabled by default
        node-version: ['22.x', '23.x']

    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Lint
        run: npm run lint
        continue-on-error: true # Don't fail on lint warnings for now

      - name: Run unit tests
        run: npm run test:unit

      - name: Check formatting
        run: npm run format:check
        continue-on-error: true # Don't fail on format issues for now

  security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run npm audit
        run: npm audit --audit-level=high
        continue-on-error: true
